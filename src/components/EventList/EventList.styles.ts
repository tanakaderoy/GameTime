import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderColor: 'black',
    borderTopWidth: 0,
    borderWidth: 1,
    width: '90%',
    maxHeight: 200,
    zIndex: -1,
  },
});

export default styles;
